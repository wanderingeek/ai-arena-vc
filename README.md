Very simple vibe-coded AI arena where the responses of two AI models are compared side by side. Gradio used for UI.

Built with OpenCode using the Hy3-free model on OpenCode Zen. Used the Gradio skill as well.
